# Tâches — add-read-at

- [x] 1. `src/read.js` : résolution de l'ancre (`msg_…` de la session, `YYYY-MM-DD`, `YYYY-MM-DDTHH:MM[:SS]`, epoch ms) + fonction de troncature de vue (messages d'horodatage ≤ ancre, inclusion à l'instant exact). Échec explicite sur ancre inconnue, ancre d'une autre session, horodatage illisible. → `resolveAnchor`, `anchorView`, `sessionSlice` (retourne `fatal: true` sur ancre invalide).
- [x] 2. `bin/sdig.js` : option `--at <ancre>` sur `read` (table `parseArgs` — piège documenté : option inconnue = erreur explicite, testé), aide du CLI mise à jour ; refus de `--at` hors de `read` (`--at : option de \`sdig read\` uniquement`), ordre des opérations : masquage, puis `--around`/`--ctx`/`--tail` dans la vue.
- [x] 3. `src/format.js` : marqueur de **masquage temporel** (compte masqué + rappel de l'ancre + « relire sans --at ») et en-tête d'ancre (`ancre : msg_… (date) — lecture bornée à cet instant, ancre incluse`) ; marqueur de troncature inchangé et non confondu (scénario « Deux signaux distincts »).
- [x] 4. `--json` sur `read` (nouveau) : champs `anchor` (id/horodatage résolus, source), `maskedCount`, `visible`, `total`, messages avec texte intégral ; `sdig raw` non filtré (scénario « La preuve reste entière »).
- [x] 5. Fenêtre entièrement postérieure à l'ancre (`--around`) → message explicite rappelant l'ancre, jamais de vide ambigu ; aucun message à/avant l'ancre → message explicite ; code de sortie 2 sur ancre invalide.
- [x] 6. Tests (`test/read.test.js`, fixture synthétique) : 10 tests ajoutés — masquage + marqueurs, visibilité sans `--at`, inclusion de l'instant exact, epoch ms et heure locale équivalents, date seule = journée entière, fenêtrage dans la vue, fenêtre postérieure, ancre inconnue / d'une autre session / illisible, JSON, preuve entière, CLI (JSON, exit 2, `--at` refusé hors `read`, option inconnue). Suite complète : **75/75**.
- [x] 7. Vérification locale (non committée) sur le corpus réel : session de n46 (`ses_f8ec348d6ffeMhsId1SrOkm67A`) — ancre à 11:31 ⇒ 21 visibles / 37 masqués, la liste de permissions (avec `bash`) est visible, la mutation de 11:38 est dehors ; lecture complète ⇒ la mutation y est. Consigné dans `eval/natural/runs/2026-09-20_read-at-verification-locale.md` (avec la nuance : ancrer sur la question masque la réponse qui documente l'état — le choix de l'ancre reste au lecteur).
- [x] 8. Documentation : `README.md` (exemple `read --at`, compteur de tests, phases v0.4/v0.5) et `openspec/implementation-plan.md` (phases v0.3 et v0.4 consignées, décisions et limite d'usage).
- [x] 9. `npm test` (75/75) + `npm run eval` (28/28 inchangé — la recherche n'est pas touchée), `openspec validate --specs --changes --strict`, puis archivage (`openspec archive add-read-at -y`).
