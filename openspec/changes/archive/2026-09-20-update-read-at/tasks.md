# Tâches — update-read-at

- [x] 1. `src/read.js` — `resolveAnchor` : validation calendaire stricte (mois 1–12, jour réel du mois avec bissextiles, heure 0–23, minute/seconde 0–59) **avant** toute construction de date ; résolution en **UTC** (`Date.UTC`) ; messages d'erreur nommant l'ancre et le motif (`ancre invalide : 2026-02-30 (jour hors bornes pour 02/2026 (28 jours))`) ; ancre vide/espaces → erreur « ancre vide ».
- [x] 2. `src/read.js` — `sessionSlice` : une ancre **fournie** (`''` inclus) n'est plus ignorée ; seul `undefined`/`null` signifie « pas d'ancrage ». Une ancre vide est donc fatale (exit 2), plus une session entière silencieuse.
- [x] 3. `bin/sdig.js` + `README.md` : aide alignée — horodatages **UTC** (comme l'affichage), refus explicites (date inexistante, heure hors bornes, ancre vide), date seule = fin de journée UTC, et précision « à la minute, l'ancre est à `:00` ; pour viser un message précis, son id ou son epoch ms ».
- [x] 4. Tests de régression (`test/read.test.js`) : `2026-02-30`, `2026-13-01`, `2026-09-00`, `2026-04-31`, `2026-09-05T25:00`, `…T10:75`, `…T10:30:61` → fatales, message nommant l'ancre ; `2026-02-29` refusée (non bissextile) et `2028-02-29` acceptée ; ancre vide (`''`, `'   '`) fatale en bibliothèque ; le test d'équivalence d'ancre a été réécrit **sans dépendance au fuseau de l'hôte** (comparaison en UTC).
- [x] 5. Tests d'indépendance du fuseau : CLI en sous-processus sous `TZ=UTC`, `TZ=Europe/Paris`, `TZ=America/New_York` avec la même ancre `2026-09-10T09:01` → ancre résolue, date affichée et vue **identiques** (2 visibles / 3 masqués) ; `--at ""` → exit 2 ; `--at 2026-02-30` → exit 2 sous un fuseau non-UTC. Suite complète relancée sous les trois fuseaux : **79/79** partout.
- [x] 6. `npm test` (79/79, trois fuseaux) + `npm run eval` (28/28 — la recherche n'est pas touchée), `openspec validate --specs --changes --strict`, puis archivage (`openspec archive update-read-at -y`). Vérification locale du corpus réel refaite et consignée dans `eval/natural/runs/2026-09-20_read-at-verification-locale.md` (non committée).
